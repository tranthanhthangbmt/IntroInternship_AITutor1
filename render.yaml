services:
  - type: web
    name: rag-app
    env: python
    buildCommand: pip install --upgrade pip && pip install -r requirements.txt
    startCommand: python test_rag.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.11
